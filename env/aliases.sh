#!/bin/bash

alias chmodx="chmod +x -R"
alias docker-exec="docker exec -it"
alias docker-run="docker run -dt --privileged"
alias docker-build="docker build --rm"
alias listen-ports="netstat -tulpn |grep LISTEN"
